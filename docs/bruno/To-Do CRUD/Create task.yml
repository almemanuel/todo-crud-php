info:
  name: Create task
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/tasks
  body:
    type: json
    data: |-
      {
        "title": "Comprar café"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
