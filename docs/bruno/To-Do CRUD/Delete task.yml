info:
  name: Delete task
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:8080/tasks/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
