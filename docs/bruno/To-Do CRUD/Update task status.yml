info:
  name: Update task status
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:8080/tasks/1
  body:
    type: json
    data: |-
      {
        "status": "concluída"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
